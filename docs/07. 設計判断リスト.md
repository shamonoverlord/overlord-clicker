07. 設計判断リスト 

A. データ構造 
1. StageDataを導入するか
概要
ステージごとの情報を1つのデータとして管理する。
例
背景
出現敵
ボス
HP倍率
BGM
エリア
判断タイミング
38.データ構造レビュー後

2. gameSettingsを独立させるか
playerDataへ含めるか、
別データとして管理するか。
例
BGM音量
SE音量
FPS
ダメージ表示
判断タイミング
38レビュー後

B. 戦闘システム
3. BossFlag方式を採用するか
Boss専用データではなく、
Enemy

↓

type = normal
type = boss
で管理するか。
判断タイミング
7・8レビュー後

4. battleDpsを持つか
内部DPS
↓
表示DPS
を分離するか。
将来のデバフ・イベント倍率対応。
判断タイミング
4レビュー後

5. processEventsを導入するか
イベントキュー方式を採用するか。
例
タップ
敵撃破
スキル終了
をイベントとして処理する。
判断タイミング
39レビュー後

C. 計算システム
6. currentStats方式を採用するか
全能力を毎回計算するのではなく、
倍率変更

↓

currentStats再生成

↓

戦闘ではcurrentStatsだけ参照
とするか。
判断タイミング
33レビュー後

7. 価格計算APIを共通化するか
例
calculateCost()
で統一するか。
または
calculateHeroCost()

calculateArtifactCost()
で分けるか。
判断タイミング
32レビュー後

D. UI
8. UIレイアウト図を仕様書へ含めるか
文章のみではなく、
ラフ
ワイヤーフレーム
スクリーンショット
を仕様書へ添付するか。
判断タイミング
29レビュー後

E. システム
9. 宝箱システムを採用するか
Tap Titansの課金宝箱ではなく、
今回ゲーム用の
ボス報酬
ログイン報酬
として採用するか。
判断タイミング
22レビュー後

10. ペットシステムを採用するか
TT2準拠で導入するか。
または
未採用とするか。
判断タイミング
18レビュー後

F. 仕様書
11. 24.アップグレードの名称変更
アップグレード
↓
能力補正
↓
倍率管理
などへ変更するか。
判断タイミング
24レビュー後

12. 課金要素を仕様書へ残すか
実装予定は無いが、
将来のため資料として残すか。
完全削除するか。
判断タイミング
37レビュー後

運用ルール
このリストはレビューが終わったあとに1項目ずつ判断する。
今決める必要はない。
レビューや実装が進む中で、
「StageDataの方が良さそうだから採用しよう」
というように決めていけば十分。
